// when u use + operator to add 2 numbers we will get summed number as output but when we add a number with a string we get a string 

console.log(3 + "hii" + 4)

// if we use - operator string is converted to number 

console.log(4 - "3")


console.log(3 && 4 && 90) // when we use && we get last truthy value if everything is truthy else we get first falsy value
console.log(3 || 4 || 5) // we get first truthy value else last value

console.log(0 || 0 || null)