(function() {
    console.log("this is immediately invoked function. It has to set of paranthesis. First one, if we dont use it compiler thinks we defined a function and the second is used to invoke it. Hence as soon as it defined, it will be invoked")
})()